# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [Unreleased]

### Added

- 文档导入、知识库管理和查询工作台
- Dense/Sparse、HyDE、RRF 与 BGE Reranker 检索链路
- 带来源引用的证据约束回答
- 自建集、RGB、CRUD-RAG、MTRAG 评测适配器
- 上传、CORS、私有对象存储和健康检查加固
- 单条聊天消息删除与当前会话记录清空，并在生成期间阻止冲突操作
- MIT License、贡献指南和安全策略

### Changed

- 技术指标使用带标题的密集行分组切片
- 重排至少保留两条合格证据，最低相关度调整为 0.09
- 重排没有合格证据但仍有召回时，最多保留八条低置信候选交给回答模型核验；完全没有候选时允许明确标识的 AI 通识回答
- 统一规范 10 个 Prompt 的注入防护、精确信息保留、部分回答、冲突处理和输出契约
- 评测使用独立会话并计算真实 P50、P95、P99
- 查询范围显式区分全部知识库、指定知识库与指定文档，联网补充默认关闭
- 整份资料总结按文档长度选择完整上下文或 Map/Reduce
- 跨网页与本地资料去重时优先本地原文，回答引用统一压缩为连续编号
- 查询规划只提取辅助主题和 `full_document`；普通检索、HyDE、联网、重排和回答统一使用当前用户原话
- 回答模型接收完整用户/助手历史与本轮检索正文，历史不再自动改变下一轮文档范围
- 显式选择文档时，正文合计不超过 64000 字符则完整直读；更长文档在重排后补充同章节相邻切片
- 没有检索候选或答案未引用候选时，允许输出明确标识的 AI 通识回答；引用不存在的编号仍拒绝整份答案
- 查询规划与 HyDE 增加 10 秒超时和无重试降级，辅助模型失败不再长期阻塞普通检索
- 新增 GitHub Actions：Push 和 Pull Request 执行无密钥离线回归与 Dockerfile 校验，`main` 分支和手动任务执行完整镜像构建

### Fixed

- 修复主体名称集合使用 `FLOAT16_VECTOR` 时，导入和检索仍传入 `float32` 导致的 Milvus 参数错误
- 修复上传区 `label` 嵌套按钮导致原生文件选择器触发迟缓或不弹出的问题
- 修复回答中的 Markdown 表格被当作普通段落显示的问题，并补充窄屏横向滚动
- 修复回答节点在候选为空时直接报错、无法使用模型通识回答的问题
- 修复查询改写偏离用户任务后继续污染检索、重排和最终回答的问题
- 修复已选择知识库全部文档时，查询规划错误要求指定单篇文档并跳过检索的问题
- 修复 HyDE 辅助模型调用无超时导致普通检索被长时间阻塞的问题；超时后自动降级为普通检索
- 修复 DashScope 关闭思考参数拼写错误，并避免向其他 OpenAI-compatible 服务发送千问专属参数
- 修复重排为 `0.0` 的完全无关候选仍进入回答上下文、诱发通识答案附加虚假资料引用的问题

## [0.1.0] - 2026-08-17

- 首个可公开发布的开发版本。
